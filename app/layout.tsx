import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "NoMarkup - Car Dealer Markup Transparency",
  description: "Report and discover car dealer markups. Find honest dealers with no-markup guarantees.",
  keywords: ["car dealers", "markup", "transparency", "no markup", "dealer reviews"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
